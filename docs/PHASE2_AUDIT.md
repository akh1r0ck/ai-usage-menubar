# Phase 2 完了監査

## 自動検証済み

| 完了条件 | 証拠 |
|---|---|
| 設定ウインドウ | `UsageSettingsWindowController`とライト／ダーク描画画像 |
| 永続化・再起動維持 | 共有`NSUserDefaults`スイートと`test-settings.sh` |
| フォント・色・太さ・しきい値 | 外観タブ、ライブプレビュー、設定モデルテスト |
| Codex／Claude個別設定 | サービス別アクセントキーと両アプリの設定画面 |
| 即時反映 | プロセス内通知と分散通知による両アプリ同期 |
| 表示形式・詳細項目 | 表示項目タブと両アプリの表示制御 |
| 更新設定 | 自動更新、間隔、ポップオーバー表示時更新、手動更新 |
| 設定初期化・移行 | 初期化、JSON書き出し／読み込みと往復テスト |
| しきい値通知の重複防止 | 利用枠識別子を含む通知済みキー |
| Claude statusLine復元 | `restore-claude-statusline.py`と保存・復元テスト |
| ライト／ダークのコントラスト | 両テーマ画像と4.5:1以上への色補正テスト |
| 複数ディスプレイ | 画面構成変更通知時のステータス項目再配置 |
| VoiceOver | Codex／Claudeステータス項目とClaudeカードのアクセシビリティラベル |
| 欠損・古いデータ | Provider／Claude parserテストと古いデータ警告 |
| README | 設定説明と設定画面画像 |
| ビルドとテスト | `make check` |

## 実機で確認が必要

以下はmacOSのユーザー設定・権限を変更するため、自動検証では完了扱いにしない。

1. ~~アプリを`/Applications`へ置き、「Macログイン時に自動起動」をオン／オフしてログイン項目へ反映されること。~~ 2026-08-19 実機確認済み。
2. ~~「通知をテスト」を押し、通知権限を許可して通知センターへ表示されること。~~ 2026-08-19 実機確認済み。

## 最終確認手順

```sh
make check
make render-settings
```

実機2項目を含むすべての完了条件を確認済み。Phase 2完了。
